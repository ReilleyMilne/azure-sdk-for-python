This file intentionally makes the fork-only Analyze stage fail.

The deterministic fix is to remove this SDK-scoped marker file.

Verification run for the hardened dispatch, compare-truncation, and
run-name correlation changes.
